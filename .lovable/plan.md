
The user approved continuing the backend wiring. Per the prior step, the next logical task is wiring the **Tickets** module to the new tables (since that was step 1 of the agreed order: Tickets → Marketing → Lookups → Reports).

Guard rails to respect:
- Do NOT change UI/functionality details — only swap the data source from `seedTickets` to Supabase.
- Keep all existing layouts, columns, filters, split-view, dialogs intact.

Scope check: `Tickets.tsx`, `CreateTicketDialog.tsx`, `AdminQueues.tsx`, `AppHeader.tsx` (urgent ticket count), and any place that imports `seedTickets`.

## Plan — Wire Tickets module to Supabase

### 1. Tickets list page (`src/pages/Tickets.tsx`)
- Replace `seedTickets` import with a `useEffect` + `supabase.from('tickets').select(...)` query joining `ticket_queues` (queue name) and `profiles` (assignee name).
- Add realtime subscription on `tickets` table so the list refreshes on insert/update/status change.
- Keep all existing filters (priority, status, queue, search), split-view, and column layout unchanged.
- Map DB enums (`P1`–`P4`, `OPEN`/`PENDING`/`RESOLVED`/`CLOSED`) to existing badge styles.

### 2. Ticket detail / split-view
- Load `ticket_messages` for the selected ticket; render existing thread UI.
- "Reply" composer → `insert into ticket_messages` (sets `is_internal` flag). The DB trigger auto-stamps `first_response_at`.
- Status / priority / assignee / queue dropdowns → `update tickets set ...`. Activity log + status timestamps handled by existing triggers.
- Attachments: upload to `ticket-attachments` bucket → insert row in `ticket_attachments`.

### 3. Create Ticket dialog (`CreateTicketDialog.tsx`)
- On submit: `insert into tickets` with subject, description, priority, category, queue, account, requester fields. `ticket_code` auto-generated by trigger.
- Compute `sla_first_response_at` and `sla_resolution_at` client-side from priority (P1=1h/4h, P2=4h/1d, P3=1d/3d, P4=2d/5d) before insert.
- Replace any seed-driven dropdowns (queue list) with live `ticket_queues` query.

### 4. Admin Queues (`src/pages/admin/AdminQueues.tsx`)
- CRUD against `ticket_queues` (name, key, description, default_assignee, is_active, sort_order).
- Default assignee picker reads from `profiles` where staff role.

### 5. Header notifications (`AppHeader.tsx`)
- Replace `seedTickets.filter(P1/P2 open)` with a live count query: `count tickets where priority in ('P1','P2') and status in ('OPEN','PENDING')`.
- Add `tickets` to the existing realtime channel so the urgent badge updates instantly.
- Dropdown items for urgent tickets → link to `/tickets?id=<id>`.

### 6. Seed data cleanup
- Remove `seedTickets` references; keep the export in `seedData.ts` only if other modules still use it (check first), otherwise delete.
- No UI/text/layout changes anywhere.

### Files
- **Edited**: `src/pages/Tickets.tsx`, `src/components/shared/CreateTicketDialog.tsx`, `src/pages/admin/AdminQueues.tsx`, `src/components/layout/AppHeader.tsx`.
- **Possibly edited**: `src/data/seedData.ts` (remove `seedTickets` if unused elsewhere).
- **No** schema changes — tables, triggers, sequence, bucket, and RLS already exist from the earlier migration.

### Out of scope
- CSAT capture UI (table supports it; UI work later).
- Ticket merging / linking.
- Marketing / Lookups / Reports wiring (next steps in the agreed order).
